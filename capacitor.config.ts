import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tokogenz.app',
  appName: 'TokoGenZ',
  webDir: 'www'
};

server: {
  androidScheme: 'https'
}

export default config;
